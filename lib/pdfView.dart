import 'package:flutter/material.dart';

class PdfView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
      title: Center(child: Text("jhjh")),
    ));
  }
}
